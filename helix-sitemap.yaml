version: 1

sitemaps:
  default:
    origin: http://main--franklin-poc--ivanviviani.hlx.live
    Lastmod: YYYY-MM-DD hh:mm:ss
    source: /query-index.json
    destination: /sitemap.xml
  it:
    origin: https://main--franklin-poc--ivanviviani.hlx.live
    Lastmod: YYYY-MM-DD hh:mm:ss
    languages:
      it:
        source: /it/it/query-index.json
        destination: /it/sitemap.xml
        hreflang: it
      en:
        source: /it/en/query-index.json
        destination: /it/sitemap.xml
        hreflang: en
        alternate: /{path}
  ch:
    origin: https://main--franklin-poc--ivanviviani.hlx.live
    Lastmod: YYYY-MM-DD hh:mm:ss
    languages:
      it:
        source: /ch/it/query-index.json
        destination: /ch/sitemap.xml
        hreflang: it
      en:
        source: /ch/en/query-index.json
        destination: /ch/sitemap.xml
        hreflang: en
        alternate: /{path}