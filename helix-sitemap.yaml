version: 1

sitemaps:
  default:
    origin: http://main--franklin-poc--ivanviviani.hlx.live
    lastmod: YYYY-MM-DD
    source: /query-index.json
    destination: /sitemap.xml
  it:
    origin: https://main--franklin-poc--ivanviviani.hlx.live/it
    lastmod: YYYY-MM-DD
    source: /it/query-index.json
    destination: /it/sitemap.xml
    languages:
      it:
        hreflang: it
        alternate: /it/{path}
      en:
        hreflang: en
        alternate: /en/{path}
  ch:
    origin: https://main--franklin-poc--ivanviviani.hlx.live/ch
    lastmod: YYYY-MM-DD
    source: /ch/query-index.json
    destination: /ch/sitemap.xml
    languages:
      it:
        hreflang: it
        alternate: /it/{path}
      en:
        hreflang: en
        alternate: /en/{path}