version: 1

sitemaps:
  it:
    origin: https://main--franklin-poc--ivanviviani.hlx.page
    Lastmod: YYYY-MM-DD hh:mm:ss
    default: it
    languages:
      it:
        source: /it/query-index.json
        destination: /it/sitemap.xml
        hreflang: it
        alternate: /it/{path}
      en:
        source: /it/query-index.json
        destination: /it/sitemap.xml
        hreflang: en
        alternate: /en/{path}
  ch:
    origin: https://main--franklin-poc--ivanviviani.hlx.page
    Lastmod: YYYY-MM-DD hh:mm:ss
    default: it
    languages:
      it:
        source: /ch/query-index.json
        destination: /ch/sitemap.xml
        hreflang: it
        alternate: /it/{path}
      en:
        source: /ch/query-index.json
        destination: /ch/sitemap.xml
        hreflang: en
        alternate: /en/{path}